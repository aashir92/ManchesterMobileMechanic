import { HeroImageAutoSlider } from "@/components/ui/image-auto-slider";

/** Standalone demo for local preview; not mounted on the public homepage. */
const DemoOne = () => {
  return (
    <div className="min-h-screen w-full bg-black">
      <HeroImageAutoSlider />
    </div>
  );
};

export { DemoOne };
